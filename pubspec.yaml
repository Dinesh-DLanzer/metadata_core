name: metadata_core
description: Core interfaces and models for MetaVision.
version: 0.0.1
repository: https://github.com/Dinesh-DLanzer/metadata_core
issue_tracker: https://github.com/Dinesh-DLanzer/metadata_core/issues

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  exif: ^3.3.0
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.9
  freezed: ^2.5.2
  json_serializable: ^6.8.0
