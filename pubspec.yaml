name: metadata_core
description: Core metadata extraction interfaces, models, and EXIF extraction tools for MetaVision and Klikr applications.
version: 0.0.6
repository: https://github.com/Dinesh-DLanzer/metadata_core
issue_tracker: https://github.com/Dinesh-DLanzer/metadata_core/issues

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  exif_reader: ^4.1.0
  flutter:
    sdk: flutter
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.9
  freezed: ^3.2.5
  json_serializable: ^6.8.0

